// Traits example - simplified for current compiler support

fn main() -> i64
    // Point coordinates
    let x1 = 3
    let y1 = 4
    let x2 = 1
    let y2 = 2

    // Manual addition (what trait methods would do)
    let sum_x = x1 + x2
    let sum_y = y1 + y2

    // Return sum of coordinates
    sum_x + sum_y
