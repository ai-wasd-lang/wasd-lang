// Generics example - simplified for current compiler support

fn main() -> i64
    // Generic struct instantiation requires GEP support
    // For now, show basic value handling

    let int_value = 42
    let other_value = 100

    // Simulating what generics would do
    int_value
