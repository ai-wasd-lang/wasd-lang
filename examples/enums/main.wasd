// Enum example - simplified for current compiler support

fn main() -> i64
    // Enum matching requires GEP support for struct construction
    // For now, show basic if-else which compiles

    let has_value = true

    let result = if has_value
        42
    else
        0

    result
