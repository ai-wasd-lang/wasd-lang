// Enum example - demonstrates enum construction and pattern matching

enum Option[T]
    Some(T)
    None

fn main() -> i64
    // Create an Option with a value
    let x = Option::Some(42)

    // Match on the option - should return 42
    let result = match x
        Option::Some(val) => val
        Option::None => 0

    result
