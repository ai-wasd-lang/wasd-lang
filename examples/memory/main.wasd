// Memory example - simplified for current compiler support

fn main() -> i64
    // Memory allocation keywords (heap, rc, arc) require GEP for structs
    // For now, show basic stack allocation

    let stack_value = 10
    let other_value = 20

    stack_value + other_value
