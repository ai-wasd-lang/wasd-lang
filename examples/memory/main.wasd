// Memory allocation example - heap, rc, arc

fn main() -> i64
    // Heap allocation
    let heap_val = heap 42

    // Reference-counted allocation
    let rc_val = rc 100

    // Atomic reference-counted allocation
    let arc_val = arc 200

    // Stack values
    let stack_val = 50

    // Return sum of values
    stack_val
